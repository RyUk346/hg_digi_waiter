DATABASE_URL=postgres://hyperglow:hyperglow_dev@localhost:5432/hyperglow
AUTH_SECRET=HyperGlowTableTop123456789012345
AUTH_URL=http://localhost:3000

# Realtime bridge (apps/realtime). Public so the browser can reach it.
NEXT_PUBLIC_REALTIME_URL=http://localhost:3001

# Sub-path deployment. Leave UNSET for local dev (serves at /).
# On the VPS where the app lives under a path, set this:
#   NEXT_PUBLIC_BASE_PATH=/SoftPOS/Test
# When set, every route is served under that prefix; Next handles
# Link/Image/router automatically.
# NEXT_PUBLIC_BASE_PATH=
